node_modules/
*.log
.env
.env.*
.DS_Store

# Rendered output. The nightly workflow publishes it as a Pages artifact; the
# repository never carries a copy, so the page cannot drift from the code.
site/
